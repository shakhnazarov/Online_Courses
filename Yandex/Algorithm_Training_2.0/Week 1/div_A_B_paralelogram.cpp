#include <iostream>
#include <algorithm>
#include <vector>
#include <cmath>
#include <map>
using namespace std;

int main(){
    int temp;

    vector<int> xs, ys;
    map<int, int> coords;
    for (int i = 0; i<8; ++i){
        cin >> temp;
        if(i%2 ==0){
            xs.push_back(temp);
        } else {
            ys.push_back(temp);
        }
    }









    double AB = sqrt(pow(x2-x1, 2) + pow(y2-y1, 2)), BC = sqrt(pow(x2-x3, 2) + pow(y2-y3, 2));
    double CD = sqrt(pow(x3-x4, 2) + pow(y3-y4, 2)), DA = sqrt(pow(x4-x1, 2) + pow(y4-y1, 2));

    if(AB*AB + CD*CD = 2())

    return 0;
}